[package]
name = "stroop-text-assembly"
description = "SAT (Stroop Assembly Text) parser"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
stroop-bytecode = { path = "../stroop-bytecode" }

[dev-dependencies]
stroop-vm = { path = "../stroop-vm" }
